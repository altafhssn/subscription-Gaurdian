# Subscription Guardian — Deployment Configuration
# Deploy to Railway: https://railway.app

[build]
  builder = "nixpacks"
  buildCommand = "pip install -r requirements.txt"

[run]
  command = "uvicorn main:app --host 0.0.0.0 --port $PORT"
